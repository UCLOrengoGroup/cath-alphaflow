/*
 *  This is a placeholder for any base config settings common to all platforms
 *  These will get overridden by platform specific settings
 *  provided nextflow is launched with "-c ./platform/<platform>/nextflow.config"
 */

 dag.overwrite = true